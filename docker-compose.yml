services:
  web:
    build:
      context: .
      args:
        DOCKERHUB_MIRROR: ${DOCKERHUB_MIRROR:-tploe6qr.mirror.aliyuncs.com}
        VITE_SUPABASE_URL: ${VITE_SUPABASE_URL}
        VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY}
    ports:
      - "8080:80"
    restart: unless-stopped
